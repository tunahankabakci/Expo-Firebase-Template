import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Background from "../components/Background";

const AlchemyScreen = () => {
  return (
    <Background>
      <View>
        <Text>AlchemyScreen</Text>
      </View>
    </Background>
  );
};

export default AlchemyScreen;

const styles = StyleSheet.create({});
